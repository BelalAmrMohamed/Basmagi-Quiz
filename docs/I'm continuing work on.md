I'm continuing work on `docs/plans/lessons-feature-plan.md`'s Phase 3 (`/create-lesson` authoring page) in the `basmagi-quiz` project. A prior session made progress; here's the exact state to resume from.

**Already done and verified correct — do not redo:**
1. **`public/src/features/home/admin-item-actions.js`** — `deleteSharedItem()` now has a `lesson` entry in `copyByType` and a dedicated `itemType === "lesson"` branch calling `postAdminAction("delete-lesson", { id: itemId })`. The rest of that file (`resolveItemType`, `resolveItemId`, `openSharedMoveToDialog`, `renameSharedItem`) already handled lessons correctly before this session touched anything — verify this is still true, but it shouldn't need changes.
2. **Sidebar nav link** — an "إنشاء دروس" link (book-open icon) was added right after the existing "إنشاء امتحانات" link in the shared sidebar markup across `public/index.html`, `public/profile.html`, `public/reports.html`, `public/result.html`, `public/settings.html`, and `public/create-quiz.html` (non-active there). Verify these insertions are intact and use CRLF line endings consistent with the rest of each file (a prior pass in this same session found and fixed an LF-vs-CRLF mismatch after a Python-based string insertion — check for that class of bug again here).
3. **`public/create-lesson.html`** — a first draft exists, assembled by reusing `create-quiz.html`'s shared shell (head/meta/OG tags reworded for lessons, anti-flash scripts, sidebar, bottom nav, avatar-badge script) with a lighter body: a simplified top bar (title + autosave indicator + preview/save buttons — deliberately no undo/redo or Docs-style menu bar, since lessons don't need bulk/reorder/template modes), an entry screen (new-lesson tile + a grid meant to be populated with the admin's own existing lessons — no local-draft concept, since lessons are Supabase-only per the plan), and a lesson editor form shell (title/course/folder/font/highlight-select metadata card, plus an empty `#sectionsContainer`). The quiz editor's question-navigator sidebar widget was repurposed into a lesson section-navigator with its own ids (`createLessonSectionNav*`) rather than reusing `question-navigator.js` (which is bound to `.question-card` DOM/scroll-observation specific to the quiz editor).
   **⚠️ This file was left mid-verification** — there was an unresolved LF/CRLF inconsistency spotted in its head section (around the copied anti-flash comment block) that was never fixed. **First step: re-check `create-lesson.html` end-to-end for line-ending consistency and balanced tags before building anything else on top of it.**

**Not started yet — build these next:**
1. **`public/src/features/lessons/create-lesson.js`** — the actual editor logic. Needs:
   - Entry-screen wiring: gate on admin auth (`isAdminAuthenticated()`/`getAdminRoleInfo()` from `adminAuth.js`); fetch the admin's own lessons directly from Supabase (`ensureSharedSupabaseClient()`) filtered by `created_by` (or list all if `roleInfo.isOwner`); render as tiles; "new lesson" tile starts a blank draft.
   - Section-list CRUD: add/remove/reorder sections. Use simple up/down move buttons for reordering rather than porting drag-and-drop machinery from `user-quizzes-folders.js` or `question-navigator.js` — neither is a close-enough fit to justify the port (this was a deliberate scoping decision made last session; feel free to revisit if there's a cleaner path, but don't default to a heavy port).
   - Per-block editors for `markdown`, `media`, `quizRef`, `question` block types (see `public/src/features/lessons/lesson-schema.js`'s `normalizeLessonContent()` and `api/_validateLesson.js` for the exact allowed shape/fields per block type — validation is strict/whitelist-based server-side, so match it exactly).
   - Markdown blocks: a plain write/preview toggle (self-built, not a port of `create-quiz.js`'s global `.gmd` floating toolbar — that toolbar is ~300+ lines of focus-tracking/toolbar machinery judged out of scope for v1). Preview renders via `renderMarkdown()` from `public/src/shared/markdown.js`, followed by the `renderMathIn()` KaTeX pattern ported verbatim from `create-quiz.js` (search for `renderMathIn` there — it's a small, self-contained function using `window.renderMathInElement`).
   - Media blocks: port a lesson-scoped version of `create-quiz.js`'s `uploadMediaFileForMarkdown()` (uploads to the `quiz-media` Supabase Storage bucket, per-admin path, with image compression) — that logic is inline in `create-quiz.js`, not in a shared module, so it needs porting rather than importing.
   - Quiz-reference (`quizRef`) picker: search `getManifest().examList` (from `public/src/shared/quizManifest.js`) by title. **Critical: the `quizId` stored on the block must be the 8-character meta id (`examList[].id`), NOT `dbId`** — confirmed by reading `lesson-view.js`'s `fetchQuizRefs()`, which looks up quizzes by `data.meta.id`, and `_urls.js`'s `quizUrl()`, which also routes on that same meta id. Using `dbId` here would silently break every quiz-ref link.
   - Embedded-question editor: prompt/options/correctIndex/explanation, structurally close to a single question in `create-quiz.js`'s question editor — port just that sub-piece, not the whole file.
   - Adaptive-rule dropdown: on a question block, a single "if wrong → reveal section: [dropdown of this lesson's other sections]" control (and optionally `onCorrect` too, per the schema) — keep to the one-rule-per-question shape `_validateLesson.js` enforces; don't build UI for multi-step chaining.
   - Save/placement wiring: call `create-lesson` / `update-lesson` admin actions (see `api/admin.js`'s `handleCreateLesson`/`handleUpdateLesson` for exact request/response shape — `{ title, content, courseId, folderId?, slug?, readerPrefsDefault? }` for create; `{ id, title, content, slug?, readerPrefsDefault? }` for update, placement change only via `move-item`, never as a side effect of a content save). Course/folder placement at creation time needs simple `<select>` dropdowns — there's no existing "pick a course+folder for a brand-new item" pattern elsewhere in the codebase to mirror (quizzes get placed via the move-to dialog after creation), so populate them via the same public Supabase read `fetchSharedDestinationNodes()` in `admin-item-actions.js` uses (courses + folders, publicly readable), built fresh here since that function is private to that module.
   - Reader-prefs defaults: font/highlight `<select>`s populated from `FONT_CHOICES`/`HIGHLIGHT_CHOICES` in `public/src/features/lessons/lesson-reader-prefs.js`, saved into `readerPrefsDefault` on the lesson row.
2. **`public/src/features/lessons/create-lesson.css`** — not started. Needs styles for the entry screen (can mirror `create-quiz.css`'s `.entry-*` class shapes conceptually, but this must be its own file per the plan, not an import of `create-quiz.css`), the simplified top bar (`.app-title-*`, but without the menu-bar classes), the metadata form, the section list, and per-block-type editor cards.
3. Once `create-lesson.js`/`.css` exist, **smoke-test the whole flow end to end** mentally against `api/_validateLesson.js`'s rules (size caps, `MAX_SECTIONS`, `MAX_BLOCKS_PER_SECTION`, unique section/question ids across the *whole* lesson not just per-section, `correctIndex` bounds, etc.) so the client never sends something the server will reject with a confusing error.

**Ground rules to keep respecting (from the plan, still binding):**
- Lessons are never scored — no points/level/`passed_quizzes_count` interaction anywhere in this editor.
- Don't touch `create-quiz.js` or `create-quiz.html` beyond what's already been done (the nav link).
- No new top-level `api/*.js` file — the admin.js lesson actions already exist and are complete; this phase is client-only.
- Vercel Hobby's 12-function cap is already accounted for; nothing in Phase 3 should need new server functions.

Please start by re-verifying `create-lesson.html`'s line endings/tag balance, then build `create-lesson.js` and `create-lesson.css`. When you're done, present the updated/created files without zipping them.