// ============================================================================
// DOM REFS — the handful of top-level elements almost every home-page module
// needs (the main content area, page title, and breadcrumb bar).
// ============================================================================
// Looked up once at module-load time, same as the original file did with its
// top-level `const container = document.getElementById(...)` declarations.

export const container = document.getElementById("contentArea");
export const title = document.getElementById("Subjects-text");
export const breadcrumb = document.getElementById("breadcrumb");
