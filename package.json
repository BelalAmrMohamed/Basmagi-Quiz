{
  "name": "basamgi-quiz",
  "version": "8.3.2",
  "description": "Vanilla JS examination platform with quiz creation and export features",
  "type": "module",
  "scripts": {
    "dev": "npm run dev:vercel",
    "dev:vercel": "concurrently -k -n vercel,sync -c blue,magenta \"npx vercel dev --local --listen 8081\" \"npx wait-on tcp:8081 && npx browser-sync start --proxy localhost:8081 --port 8080 --files 'public/**/*.html,public/src/**/*.js,public/src/**/*.css,api/**/*.js' --reload-delay 250 --no-open\"",
    "dev:static": "npx http-server public -p 8080 -o -c-1",
    "vercel": "vercel dev",
    "map": "node scripts/map.js",
    "commit": "node scripts/commit.js",
    "zip": "git archive --format=zip --output=basmagi-quiz.zip HEAD",
    "seo:check": "node scripts/seo-check.mjs"
  },
  "keywords": [
    "examination",
    "quiz",
    "education",
    "vanilla-js"
  ],
  "author": "Belal Amr Mohamed",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^10.0.5",
    "http-server": "^14.1.1",
    "wait-on": "^9.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.81.1",
    "@vercel/og": "^0.11.1",
    "formidable": "^3.5.4",
    "ignore": "^7.0.8",
    "jsonwebtoken": "^9.0.3",
    "mammoth": "^1.12.2"
  }
}